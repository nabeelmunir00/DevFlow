export function WorkspaceHeader() {
  return (
    <header className="sticky top-0 z-30 h-16 border-b border-border bg-background">
      <span className="sr-only">Workspace header</span>
    </header>
  );
}
